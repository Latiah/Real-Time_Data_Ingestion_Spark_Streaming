[project]
name = "realtime-spark-postgres"
version = "0.1.0"
description = "Real-time data ingestion pipeline demonstrating Spark Structured Streaming, data transformation, and PostgreSQL storage."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
