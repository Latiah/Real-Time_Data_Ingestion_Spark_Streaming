-- Run this once, connected as a superuser (e.g. `psql -U postgres`).
-- Creates the `realtime_events` database if needed and then creates the
-- `events` table inside that database.


CREATE DATABASE realtime_events;
